# Prototype Workspace

本目录用于承载重新立项后的 Web 原型，不再混用旧版 `web-prototype/`。

## 目录

| 路径 | 状态 | 用途 |
| --- | --- | --- |
| `v0.2.0-planning/` | active | 需求评审阶段 Web 原型 demo |

旧原型仍保留在仓库根目录的 `web-prototype/`，归属 `v0.1.0-legacy`，只作为参考资料。
