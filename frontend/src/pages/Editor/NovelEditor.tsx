import { Typography } from 'antd'

const { Title } = Typography

const NovelEditor = () => {
  return (
    <div>
      <Title level={2}>小说编辑器</Title>
      <p>小说编辑器页面 - 待实现</p>
    </div>
  )
}

export default NovelEditor
